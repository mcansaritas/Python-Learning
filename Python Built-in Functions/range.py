"""the built-in range() method returns a sequence of numbers, starting from 0 by default, and
increments by 1 (by default), and stops before a specified number. The syntax is
range(start, stop, step)."""

x = range(6)
for values in x:
    print(values)

# This will print out the values from 0 to the 5
