"""The built-in round() method returns a floating point number that is a rounded version of the
specified number, with the specified number of decimals. The default number of decimal is 0,
meaning that the function will return the nearest integer. The syntax is round(number, digits)."""

x = round(5.76543, 2)
print(x)  # This will round the floating number using the 2 digits after comma
