"""The built-in list() method creates a list object. A list object is a collection which is
ordered and changeable. The syntax is list(iterable)."""

x = list(('apple', 'banana', 'cherry'))
print(x)

# First we had a tuple, then with list method we convert it into a list