"""The built-in hex() function converts the specified number into a hexadecimal value. The returned
string always starts with the prefix '0x'. The syntax is hex(number)."""

x = hex(255)
y = hex(16)

print(x)  # It returns the decimal value of 255 as hexadecimal
print(y)  # It returns the decimal value of 16 as hexadecimal
