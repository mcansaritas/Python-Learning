"""The built-in format() method formats a specified value into a specified format. The syntax is
format(value, format)."""

x = format(0.25, '%')
print(x)

"""There are some legal values that you can use to format the data."""