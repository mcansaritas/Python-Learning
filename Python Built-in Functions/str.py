"""The built-in str() method converts the specified value into a string. The syntax is
 str(object, encoding=encoding, errors=errors)."""

x = str(3.5)
print(x)  # We can see that it prints out 3.5
print(type(x))  # We can see that it is class str
