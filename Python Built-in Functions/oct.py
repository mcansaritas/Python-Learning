"""The built-in oct() method converts an integer into an octal string. Octal string in Python
are prefixed with 0o. The syntax is oct(int)."""

x = oct(12)
print(x)  # It converts 12 with 8 base and prints out
