"""The built-in chr() method returns the character represents the specified unicode. We need to
specify an integer representing a valid unicode code point."""

x = chr(97)
y = chr(120)
z = chr (112)

print(x, y, z)

# It will print the unicode to character mapping based on the input
