"""The built-in method dict() creates a dictionary. A dictionary is a collection which is not
ordered, changeable, and indexed. The syntax how we can write it dict(keyword arguments)."""

x = dict(name = "Mertcan", age = 27, country = "Turkey")
print(x)

# We can see that our variable and value mappings are converted to a dictionary.
