"""The built-in set() method creates a set object. The items in a set list are unordered, so it
will appear in random order. The syntax is set(iterable)."""

x = set(('apple', 'banana', 'cherry'))
print(x)  # It prints our the tuple as a set
